C
C
	REAL FUNCTION ARCTAND(Y,X)
C       REVISED BY: DGL NOV 2005
C        + changed atan2d to atan2 with constant
	IF (Y.EQ.0.0.AND.X.EQ.0.0) THEN
		ARCTAND=0.0
	ELSE
		ARCTAND=ATAN2(Y,X)*57.2957795
	ENDIF
	RETURN
	END
